# organization_app_starter

A new Flutter project.

## Flutter Version

This project requires **Flutter 3.41.1**.

With FVM: run `fvm use` (reads `.fvmrc` automatically).
Without FVM: install manually from https://docs.flutter.dev/release/archive
